# Copy to .env.local for local dev (it's gitignored).

# Public URL of the Go API. On Vercel set this to your deployed API URL
# (e.g. https://aptly-api.onrender.com). If unset, the app falls back to
# http://localhost:8080 for local development.
NEXT_PUBLIC_API_URL=http://localhost:8080
